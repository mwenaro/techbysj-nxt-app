// import { Footer, Header } from '../(components)';

// export const metadata = {
//   title: 'Techbysj - ',
//   description: 'Sample Page',
// };

export default function CourseBookingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <> {children}</>;
}
